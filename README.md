# AI Community Platform

This repository is currently in a documentation-first phase and now includes a stub-first local Docker Compose runtime for early infrastructure validation.

For startup instructions, default local credentials, and post-setup credential rotation, see [docs/local-dev.md](docs/local-dev.md).
